#!/bin/sh

echo "-----> Solc Build started"

BUILD_DIR="$1"
CACHE_DIR="$2"

# Add Ethereum PPA
add-apt-repository -y ppa:ethereum/ethereum
apt-get update

# Install solc
apt-get install -y solc

# Check solc version
solc --version
